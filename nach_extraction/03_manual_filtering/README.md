# Manual Filtering Scripts

- `fasta_sortandclean.py`: sort and clean FASTA files before downstream prediction steps
- `spec_sampID_rename.sh`: standardize species and sample identifiers

Need to manually review sequences in Geneious still. (add instructions)